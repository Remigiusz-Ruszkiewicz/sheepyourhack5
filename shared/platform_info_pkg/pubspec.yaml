name: platform_info_pkg
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  device_info_plus: ^9.0.3
  mocktail: ^1.0.1

  # Modules
  dependency_interfaces:

dev_dependencies:
  flutter_test:
    sdk: flutter
  test: ^1.24.3
